# include <stdio.h>

int main(){

    // Program to check whether a number is divisible by 97 or not.

    int num;
    printf("Enter a number : ");
    scanf("%d", &num);

    printf("Divisibility test returns : %d \n", num % 97);

    // Solve a sum

    int x = 2, y = 3, z = 3, k = 1;

    printf("The value of 3 * x / y - z + k is : %d \n", 3 * x / y - z + k);

    // Solve a sum

    printf("The sum of 3.0 & 5 is : %f \n", 3.0 + 5);

    return 0;
}